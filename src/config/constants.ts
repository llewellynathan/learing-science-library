/**
 * Maximum number of images allowed per section in the AI-assisted audit.
 * The Claude API supports up to 20 images per request, so this is a UX/cost control limit.
 */
export const MAX_IMAGES_PER_SECTION = 10;
